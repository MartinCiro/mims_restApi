apiVersion: v1
kind: ConfigMap
metadata:
  name: go-api-config
  labels:
    app: go-api
    source: env-file
data:
  # Application Configuration
  APP_NAME: "SoftCalFut API"
  APP_ENV: ""
  APP_PORT: "4000"
  
  # Server Configuration
  SERVER_HOST: "0.0.0.0"
  SERVER_PORT: "4000"
  
  # Database Configuration
  HOST_DB: "postgres-service"
  PORT_DB: "5432"
  USER_DB: "postgres"
  NAME_DB: "myapp"
  
  # Redis Configuration
  REDIS_URL: "redis-service"
  REDIS_PORT: "6379"
  
  # JWT Configuration
  JWT_EXPIRES_IN: "24h"
  
  # Application Settings
  LOG_LEVEL: "info"
  CORS_ALLOWED_ORIGINS: "*"
