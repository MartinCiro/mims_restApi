apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-api-deployment-1
  labels:
    instance: "1"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: go-api
      tier: backend
      instance: "1"
  template:
    metadata:
      labels:
        app: go-api
        tier: backend
        instance: "1"
    spec:
      containers:
      - name: go-api
        image: go-api:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 4000
        env:
        # Variables desde ConfigMap
        - name: APP_ENV
          valueFrom:
            configMapKeyRef:
              name: go-api-config-1  # ← Actualizado para instancia 1
              key: APP_ENV
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: go-api-config-1  # ← Actualizado para instancia 1
              key: APP_PORT
        - name: HOST_DB
          valueFrom:
            configMapKeyRef:
              name: go-api-config-1  # ← Actualizado para instancia 1
              key: HOST_DB
        - name: PORT_DB
          valueFrom:
            configMapKeyRef:
              name: go-api-config-1  # ← Actualizado para instancia 1
              key: PORT_DB
        - name: USER_DB
          valueFrom:
            configMapKeyRef:
              name: go-api-config-1  # ← Actualizado para instancia 1
              key: USER_DB
        - name: NAME_DB
          valueFrom:
            configMapKeyRef:
              name: go-api-config-1  # ← Actualizado para instancia 1
              key: NAME_DB
        - name: REDIS_URL
          valueFrom:
            configMapKeyRef:
              name: go-api-config-1  # ← Actualizado para instancia 1
              key: REDIS_URL
        - name: REDIS_PORT
          valueFrom:
            configMapKeyRef:
              name: go-api-config-1  # ← Actualizado para instancia 1
              key: REDIS_PORT

        # Secrets
        - name: PASS_DB
          valueFrom:
            secretKeyRef:
              name: app-secrets-1  # ← Actualizado para instancia 1
              key: DB_PASSWORD
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: app-secrets-1  # ← Actualizado para instancia 1
              key: REDIS_PASSWORD
        - name: JWT_SECRETO
          valueFrom:
            secretKeyRef:
              name: app-secrets-1  # ← Actualizado para instancia 1
              key: JWT_SECRET
        - name: JWT_SALT
          valueFrom:
            secretKeyRef:
              name: app-secrets-1  # ← Actualizado para instancia 1
              key: JWT_SALT
              
        # Variables calculadas
        - name: DB_URL
          value: "postgresql://$(USER_DB):$(PASS_DB)@$(HOST_DB):$(PORT_DB)/$(NAME_DB)?sslmode=disable"
        - name: REDIS_URL
          value: "redis://:$(REDIS_PASSWORD)@$(REDIS_URL):$(REDIS_PORT)"

        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 4000
          initialDelaySeconds: 45
          periodSeconds: 60
        readinessProbe:
          httpGet:
            path: /ready
            port: 4000
          initialDelaySeconds: 10
          periodSeconds: 60